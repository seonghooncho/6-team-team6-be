spring:
  flyway:
    locations: classpath:db/migration

---
spring:
  config:
    activate:
      on-profile: local,dev

  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1

logging:
  level:
    org.flywaydb: INFO

---
spring:
  config:
    activate:
      on-profile: prod
  flyway:
    enabled: false
    baseline-on-migrate: true
    baseline-version: 1
    clean-disabled: true

logging:
  level:
    org.flywaydb: WARN
